<%- include('../partials/header') -%>
<br>
<div style="padding-left: 35%;">
  <span > <img src="/images/storyicon.png" wdith="50" height="50" style="float: left;"> <h1 style="text-align: center;"><h1><sub>Welcome to the storytelling world</sub></h1>
  </span>
  </p>
</div>
<br>
<%- include('../partials/navbar') -%>
<div class="container" >
  
    <div class="row mt-3">
<div class="col md-12">
    <%- include('../partials/flashMessages') -%>
</div>      
    
    </div>

  <% chunk.forEach((events)=> {%>
    <div class="row mt-3">

   
        <% events.forEach((event)=> {%>
          <div class="col-sm-6 col-md-4">
              <div class="card card-style centertext">
                  
                  <div class="card-body">
                    <h4 class="card-title "><%= event.title%></h4>
                    <!-- <p class="card-text"><%= event.description%></p>
                    <p><%= event.location%></p> -->
                    
                    <img src="<%= '/images/' + event.avatar%>"  height="100px" width="100px">
                    <br>
                    <br>
                    <a href="<%=  '/events/show/'  + event.id %>" class="btn btn-primary button-style ">Read story</a>


                  </div>
                </div>
          </div>
          <%})%>
    </div>

  <%})%>
<div class="row">
  <div class="col-12 mt-2 mb-4">
    <nav aria-label="...">
      <ul class="pagination pagination-lg justify-content-center">
        <% for (let i=1 ; i <= Math.ceil(total/6); i++) {%>
        
          <% if (pageNo == i) {%>
            <li class="page-item active"><a class="page-link" href="<%= '/events/' + i%>"><%= i%></a></li>
          <%} else { %>
          <li class="page-item"><a class="page-link" href="<%= '/events/' + i%>"><%= i%></a></li>
          <%}%>
        
        <%}%>
      </ul>
    </nav>
  </div>
</div>
</div>


<%- include('../partials/footer') -%>

